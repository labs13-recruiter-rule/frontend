import axios from 'axios';
import {} from './types';

// TOKEN

const token = sessionStorage.getItem('token');
const tokenHeader = {
  headers: {
    token: token,
  },
};

// GET ALL ENGINES

//GET ENGINE BY ID

// ADD ENGINE

// EDIT ENGINE

// DELETE ENGINE
