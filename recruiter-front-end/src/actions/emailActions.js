import axios from 'axios';

// TOKEN

const token = sessionStorage.getItem('token');
const tokenHeader = {
  headers: {
    token: token,
  },
};

// GET EMAIL HISTORY

// ADD EMAIL HISTORY

// SEND EMAIL
